(ns rod
  (:require [clojure.string :as str]
            [test]))

(defn hello [s]
  (if (> (+ 1 2) 1)
    (println "Hello," s \newline
             (str/test-str) \newline
             (test/another-test-str))
    (println "This shouldn't print!")))

(hello "Rod")
